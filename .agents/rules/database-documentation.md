# Base de Datos y Prisma

Siempre que modifiques el esquema de la base de datos (`prisma/schema.prisma`), debes mantener actualizada la documentación centralizada de la base de datos en `docs/database_schema.md`.

Esta regla garantiza que el resto del equipo tenga visibilidad sobre los cambios en la BD.
